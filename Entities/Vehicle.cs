namespace Aula192_solucaoSemInterface.Entities
{
    internal class Vehicle
    {
        public string Model { get; set; } //classe veiculo

        public Vehicle(string model)
        {
            Model = model;
        }
    }
}